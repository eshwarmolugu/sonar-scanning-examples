pipeline {
  agent any
  tools {
       maven 'Maven'
        }
  stages {
       stage('Build') {
           steps {
		echo 'Stage Build'
		npm install .
                //sh 'mvn -B -DskipTests clean compile'
                }
            }
       stage('Test') {
           steps {
		echo 'Stage Test'
                //sh 'mvn test'    
	            }
            }
      stage('Deploy') {
           steps {
              echo 'Steage Deploy '
                }
            }
        }
}
